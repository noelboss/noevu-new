---
import type { Navigation } from '../../schemas/navigation';
import type { SiteConfig } from '../../schemas/site';

interface Props {
  navigation: Navigation;
  site: SiteConfig;
}

const { navigation, site } = Astro.props;
const { footer, social } = navigation;
const { company, contact } = site;

const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer__container container">
    <div class="footer__main">
      <!-- Brand Column -->
      <div class="footer__brand">
        <a href="/" class="footer__logo" aria-label="Noevu - Zur Startseite">
          <img src="/images/brand/noevu-logo.png" alt="Noevu" class="footer__logo-img" />
        </a>
        <address class="footer__address">
          <p class="footer__company">{company.legalName}</p>
          <p>{contact.address.street}</p>
          <p>{contact.address.zip} {contact.address.city}</p>
          <p>{contact.address.country || 'Schweiz'}</p>
        </address>
      </div>

      <!-- Contact Column -->
      <div class="footer__column">
        <h3 class="footer__column-title">
          <span class="footer__dot"></span>
          Kontakt
        </h3>
        <ul class="footer__links">
          <li>
            <a href={`tel:${contact.phone.replace(/\s/g, '')}`} class="footer__link footer__link--contact">
              <span class="material-symbols-outlined">call</span>
              {contact.phone}
            </a>
          </li>
          <li>
            <a href={`mailto:${contact.email}`} class="footer__link footer__link--contact">
              <span class="material-symbols-outlined">mail</span>
              {contact.email}
            </a>
          </li>
          <li>
            <a href="/termin" class="footer__link footer__link--contact">
              <span class="material-symbols-outlined">calendar_month</span>
              Termin buchen
            </a>
          </li>
        </ul>
      </div>

      <!-- Navigation Columns -->
      {footer.columns.map((column) => (
        <div class="footer__column">
          <h3 class="footer__column-title">
            <span class="footer__dot"></span>
            {column.title}
          </h3>
          <ul class="footer__links">
            {column.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="footer__link"
                  target={link.external ? '_blank' : undefined}
                  rel={link.external ? 'noopener noreferrer' : undefined}
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <!-- Bottom Bar -->
    <div class="footer__bottom">
      <div class="footer__bottom-left">
        <!-- Partner Badge -->
        <div class="footer__badge">
          <a href="/schweizer-squarespace-circle-gold-partner-2025" aria-label="Squarespace Circle Gold Partner">
            <img
              src="https://images.squarespace-cdn.com/content/v1/5134cbefe4b0c6fb04df8065/1674050553879-K2J82XLSJR2TFPFFN4VW/Circle_Partner_Logo_Gold.png"
              alt="Squarespace Circle Gold Partner"
              width="80"
              height="40"
              loading="lazy"
            />
          </a>
        </div>

        <!-- Social Links -->
        {social.length > 0 && (
          <div class="footer__social">
            {social.map((item) => (
              <a
                href={item.href}
                class="footer__social-link"
                target="_blank"
                rel="noopener noreferrer"
                aria-label={item.label || item.platform}
              >
                {item.platform === 'linkedin' && (
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                    <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
                  </svg>
                )}
                {item.platform === 'facebook' && (
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                    <path d="M9 8h-3v4h3v12h5v-12h3.642l.358-4h-4v-1.667c0-.955.192-1.333 1.115-1.333h2.885v-5h-3.808c-3.596 0-5.192 1.583-5.192 4.615v3.385z"/>
                  </svg>
                )}
                {item.platform === 'instagram' && (
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                    <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                  </svg>
                )}
              </a>
            ))}
          </div>
        )}
      </div>

      <div class="footer__bottom-center">
        <p class="footer__copyright">
          &copy; {currentYear} {company.legalName}. Alle Rechte Vorbehalten.
        </p>
        <nav class="footer__legal" aria-label="Rechtliches">
          <a href="/datenschutz-impressum">Datenschutz & Impressum</a>
          <span class="footer__legal-sep">–</span>
          <a href="#" data-cookie-settings>Cookie-Einstellungen</a>
          <span class="footer__legal-sep">–</span>
          <a href="/llms">Infos für LLMs</a>
        </nav>
      </div>

      <div class="footer__bottom-right">
        <div class="footer__lang">
          <span class="material-symbols-outlined footer__lang-icon">translate</span>
          <a href="/de" class="footer__lang-link footer__lang-link--active">Deutsch</a>
          <a href="/en" class="footer__lang-link">English</a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  /* ═══════════════════════════════════════════════════════════════
     FOOTER - Dark section with gradient background
     Matching original noevu.ch footer design
     ═══════════════════════════════════════════════════════════════ */
  .footer {
    position: relative;
    color: white;
    padding-block: 5rem 2.5rem;
    overflow: hidden;
  }

  /* Dark gradient background matching original */
  .footer::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(0deg, #141110 0%, #235f52 100%);
    z-index: -1;
  }

  .footer__container {
    position: relative;
    z-index: 1;
  }

  .footer__main {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    margin-bottom: 4rem;
  }

  @media (min-width: 640px) {
    .footer__main {
      grid-template-columns: repeat(2, 1fr);
      gap: 2.5rem;
    }
  }

  @media (min-width: 1024px) {
    .footer__main {
      grid-template-columns: 1.5fr 1fr repeat(3, 1fr);
      gap: 2rem;
    }
  }

  /* ═══════════════════════════════════════════════════════════════
     BRAND COLUMN
     ═══════════════════════════════════════════════════════════════ */
  .footer__brand {
    grid-column: 1;
  }

  @media (min-width: 640px) {
    .footer__brand {
      grid-column: span 2;
    }
  }

  @media (min-width: 1024px) {
    .footer__brand {
      grid-column: 1;
    }
  }

  .footer__logo {
    display: inline-block;
    text-decoration: none;
    margin-bottom: 1.5rem;
    transition: opacity 0.3s ease;
  }

  .footer__logo:hover {
    opacity: 0.85;
  }

  .footer__logo-img {
    height: 2.5rem;
    width: auto;
    display: block;
    filter: brightness(0) invert(1); /* Make logo white */
  }

  .footer__address {
    font-style: normal;
    font-size: 0.9375rem;
    line-height: 1.8;
    color: hsla(0, 0%, 100%, 0.75);
  }

  .footer__address p {
    margin: 0;
  }

  .footer__company {
    font-weight: 600;
    color: white;
    margin-bottom: 0.375rem !important;
  }

  /* ═══════════════════════════════════════════════════════════════
     NAVIGATION COLUMNS
     ═══════════════════════════════════════════════════════════════ */
  .footer__column {
    min-width: 0;
  }

  .footer__column-title {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 600;
    color: white;
    margin-bottom: 1.25rem;
    text-transform: none;
    letter-spacing: 0;
  }

  /* Orange glowing dot - matching original noevu.ch */
  .footer__dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    background: var(--color-orange, #ff6b30);
    border-radius: 50%;
    box-shadow: 0 0 6px hsla(17, 100%, 59%, 0.75);
    transition: box-shadow 0.2s ease;
  }

  .footer__column-title:hover .footer__dot {
    box-shadow: 0 0 8px var(--color-orange, #ff6b30);
  }

  .footer__links {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer__links li {
    margin-bottom: 0.5rem;
  }

  /* Link styling with hover underline - matching dark section links */
  .footer__link {
    display: inline-block;
    font-size: 0.875rem;
    color: hsla(0, 0%, 100%, 0.75);
    text-decoration: underline;
    text-decoration-color: hsla(0, 0%, 100%, 0.15);
    text-underline-offset: 4px;
    text-decoration-thickness: 1px;
    transition:
      color 0.2s ease-in-out,
      text-decoration-color 0.2s ease-in-out;
    padding-block: 0.1875rem;
  }

  .footer__link:hover {
    color: hsla(35, 27%, 94%, 1); /* Beige on hover - matching original */
    text-decoration-color: hsla(35, 27%, 94%, 1);
  }

  /* Contact links with icons */
  .footer__link--contact {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    text-decoration: none;
    text-decoration-color: transparent;
  }

  .footer__link--contact .material-symbols-outlined {
    font-size: 1.125rem;
    opacity: 0.6;
    transition: opacity 0.2s ease;
  }

  .footer__link--contact:hover {
    text-decoration: underline;
    text-decoration-color: hsla(35, 27%, 94%, 1);
    text-underline-offset: 4px;
  }

  .footer__link--contact:hover .material-symbols-outlined {
    opacity: 1;
  }

  /* ═══════════════════════════════════════════════════════════════
     BOTTOM BAR
     ═══════════════════════════════════════════════════════════════ */
  .footer__bottom {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding-top: 2.5rem;
    border-top: 1px solid hsla(0, 0%, 100%, 0.12);
  }

  @media (min-width: 1024px) {
    .footer__bottom {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
  }

  .footer__bottom-left {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  /* Partner Badge */
  .footer__badge {
    position: relative;
  }

  .footer__badge img {
    height: 36px;
    width: auto;
    opacity: 0.85;
    filter: brightness(0) invert(1);
    transition: opacity 0.3s ease;
  }

  .footer__badge:hover img {
    opacity: 1;
  }

  /* ═══════════════════════════════════════════════════════════════
     SOCIAL ICONS - with hover effects
     ═══════════════════════════════════════════════════════════════ */
  .footer__social {
    display: flex;
    gap: 0.5rem;
  }

  .footer__social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    color: white;
    background: hsla(0, 0%, 100%, 0.08);
    border-radius: 50%;
    transition:
      background 0.3s ease,
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  .footer__social-link:hover {
    background: hsla(0, 0%, 100%, 0.15);
    transform: translateY(-2px);
    box-shadow:
      0 4px 12px hsla(0, 0%, 0%, 0.3),
      0 0 20px hsla(0, 0%, 100%, 0.1);
  }

  .footer__social-link svg {
    width: 1.125rem;
    height: 1.125rem;
    transition: transform 0.3s ease;
  }

  .footer__social-link:hover svg {
    transform: scale(1.1);
  }

  /* Center Section */
  .footer__bottom-center {
    text-align: center;
  }

  @media (min-width: 1024px) {
    .footer__bottom-center {
      text-align: left;
    }
  }

  .footer__copyright {
    font-size: 0.8125rem;
    color: hsla(0, 0%, 100%, 0.5);
    margin: 0 0 0.5rem 0;
  }

  .footer__legal {
    display: flex;
    gap: 0.625rem;
    font-size: 0.8125rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (min-width: 1024px) {
    .footer__legal {
      justify-content: flex-start;
    }
  }

  .footer__legal a {
    color: hsla(0, 0%, 100%, 0.5);
    text-decoration: underline;
    text-decoration-color: hsla(0, 0%, 100%, 0.2);
    text-underline-offset: 3px;
    text-decoration-thickness: 1px;
    transition:
      color 0.2s ease-in-out,
      text-decoration-color 0.2s ease-in-out;
  }

  .footer__legal a:hover {
    color: hsla(35, 27%, 94%, 1);
    text-decoration-color: hsla(35, 27%, 94%, 1);
  }

  .footer__legal-sep {
    color: hsla(0, 0%, 100%, 0.3);
  }

  /* Right Section */
  .footer__bottom-right {
    display: flex;
    justify-content: center;
  }

  @media (min-width: 1024px) {
    .footer__bottom-right {
      justify-content: flex-end;
    }
  }

  /* ═══════════════════════════════════════════════════════════════
     LANGUAGE SWITCHER - matching original noevu.ch design
     ═══════════════════════════════════════════════════════════════ */
  .footer__lang {
    display: flex;
    align-items: center;
    gap: 0.5em; /* matching original em units */
    font-size: 1rem; /* matching original 1rem */
  }

  .footer__lang-icon {
    font-size: 1.25rem;
    opacity: 0.5;
    transition: opacity 0.2s ease;
  }

  .footer__lang:hover .footer__lang-icon {
    opacity: 0.7;
  }

  .footer__lang-link {
    color: hsla(0, 0%, 100%, 0.5);
    text-decoration: none;
    transition:
      color 0.2s ease-in-out,
      opacity 0.2s ease-in-out;
  }

  .footer__lang-link:hover {
    color: white;
  }

  .footer__lang-link--active {
    color: white;
    text-decoration: underline;
    text-underline-offset: 4px;
    opacity: 1;
  }
</style>
