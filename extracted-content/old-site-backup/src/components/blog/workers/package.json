{
  "name": "blog-html-rewriter",
  "version": "1.0.0",
  "description": "Cloudflare Worker for server-side HTML transformations of blog content",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
